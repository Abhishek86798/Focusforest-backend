services:
  - type: web
    name: focusforest-backend
    runtime: node
    buildCommand: npm ci && npm run build && node node_modules/.bin/prisma generate
    startCommand: npm start
    envVars:
      - key: NODE_ENV
        value: production
